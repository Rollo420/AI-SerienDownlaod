# Verwende das offizielle Selenium Standalone Chromium Image als Basis
FROM selenium/standalone-chromium:latest

# Keine weiteren Befehle hier nötig, da das Basis-Image bereits konfiguriert ist.
# Umgebungsvariablen und Ports werden über die docker-compose.yml gesteuert.
